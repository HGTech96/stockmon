Phase 23b: per-user data isolation

Splits stocks into shared tickers/daily_prices plus a new per-user
watchlist_entries table, and scopes trades/cash/settings/refresh_status 
by user_id — every route but the (intentionally global) screener now
requires login and returns only the logged-in user's data.